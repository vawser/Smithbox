// Automatically Generated

namespace HKLib.hk2018;

// By default this passes the object type to the parent rather than the VALUE parameter.
// Passing the VALUE parameter instead leads to the intended behavior, MapTuple cannot be made covariant as it must be a class
public class hkHashMap<KEY, VALUE> : hkHashBase<hkHashMapDetail.MapTuple<KEY, VALUE>> { }