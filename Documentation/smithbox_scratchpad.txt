#---------------------
# TODO 
#---------------------
- Implement new Settings impl

- Update the code structuring in Param Editor to match the Foundry implementation
- Add 'Variant Generator' that implements a generic framework that meets the requirements of the GenerateAffinity script: i.e. define instance count, the fields to modify for each count index (include abs/relative operations)

- Implement FMG Descriptor pattern for Text Editor
- Implement Views for Text Editor

public static PreferenceItem XXX()
{
    return new PreferenceItem
    {
        Category = PreferenceCategory.System,
        Spacer = true,

        Section = "General",

        Title = "XXX",
        Description = "XXX",

        Draw = () => {
            ImGui.Checkbox("##inputValue", ref CFG.Current.System_Check_Program_Update);
        }
    };
}