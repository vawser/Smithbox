#--------------------------------------
# TODO
#--------------------------------------
- Add option to store imgui.ini in AppData.
- Add option to replace install dir imgui.ini with stored one from AppData.

- Update Andre.Param to include the signed bitfield changes from SoulsFormats 

- Correct row index support in the Param Editor in places where it doesn't function correctly (e.g. row name import)

- Change Name lists to a JSON format:
 - Holds row ID, row index, source name and community name 
 (source being leaked row name)
- Update name export options so it is simple to update the name lists via editing the row names in the Param Editor and then exporting
 
- Update AC6 paramdex to use released AC6 paramdefs + adjust other stuff based on discoveries

